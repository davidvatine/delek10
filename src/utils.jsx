// ── כלים ── import React,{useState}from"react"; import{PU,WH,BL,APP_LINK,DISCLAIMER,MHE}from"./constants.js"; export const SLogo=({src,alt,style})=>{ const[e,sE]=useState(false); if(e||!src)return<div style={{...style,display:"flex",alignItems:"center",justifyContent:"center",background:PU,color:WH,fontWeight:"bold",fontSize:(style.width||40)/3,borderRadius:"8px"}}>{alt}</div>; return<img src={src} alt={alt} style={style} onError={()=>sE(true)}/>; }; export const BADGE={ "שני חסכוני":["#DBEAFE","#1565C0"], "חג / אירוע":["#DCFCE7","#166534"], "מצחיק / אפליקציה":["#FEF3C7","#92400E"], "דרושים":["#FCE7F3","#9D174D"], "פוסט מבצע":["#FFE4E6","#9F1239"], }; export const Badge=({t})=>{const[bg,c]=BADGE[t]||["#F3F4F6","#374151"];return<span style={{padding:"3px 10px",borderRadius:20,fontSize:11,fontWeight:800,background:bg,color:c,whiteSpace:"nowrap"}}>{t}</span>;}; export function dayName(d,m,y){return["ראשון","שני","שלישי","רביעי","חמישי","שישי","שבת"][new Date(y,m-1,d).getDay()];} export async function callAI(prompt){ try{ const r=await fetch("/api/ai",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({prompt})}); if(!r.ok)throw new Error(); const d=await r.json(); return(d.text||"").trim(); }catch{return"שגיאה. נסה שוב.";} } export function buildPrompt(type,ctx,date,day,promoText=""){ const{season,weather,holidays,news,emoji}=ctx; const base=`אתה כותב פוסטים לדף הפייסבוק הרשמי של דלק Ten - רשת תחנות דלק ישראלית. הקהל: נהגים ישראלים, מכל הגילאים ורקעים. טון: חם, ישראלי, אנושי, עם הומור עדין. עונה: ${season} | מזג אוויר: ${weather} ${emoji} חגים/אירועים: ${holidays||"ללא"} הקשר תקשורתי: ${news}${ctx.extra?` | ${ctx.extra}`:""} תאריך הפוסט: ${date||"לפי מבצע"}${day?" (יום "+day+")":""} פורמט חובה: 3-5 פסקאות קצרות עם רווחים ביניהן, אמוג'ים רלוונטיים, CTA ברורה בסוף. `; if(type==="שני חסכוני")return base+`כתוב פוסט יום שני חסכוני. הדגש: 40 אגורות חיסכון לליטר בנזין בתשלום באפליקציה או כרטיס TenVIP. קשר לעונה/חגים. סיים עם: "עוד אין לכם את האפליקציה? 👇 ${APP_LINK}\n${DISCLAIMER}"`; if(type==="חג / אירוע")return base+`כתוב פוסט לחג/אירוע: ${holidays||season}. קשר אישי ואנושי לחג, Ten כאן בשבילכם. אזכור עצירה בתחנה. סיים עם: "${DISCLAIMER}"`; if(type==="מצחיק / אפליקציה")return base+`כתוב פוסט הומוריסטי על האפליקציה. שאלה/פול לעוקבים. הדגש נוחות: חסכון בזמן, תשלום קל. סיים עם לינק: ${APP_LINK}`; if(type==="דרושים")return base+`כתוב פוסט גיוס עובדים לתחנות Ten. צוות משפחתי, תנאים טובים, גמישות. CTA: שלחו הודעה.`; if(type==="פוסט מבצע")return base+`כתוב פוסט מבצע מיוחד. פרטי המבצע: ${promoText||"מוצר/שירות מיוחד בתחנות Ten"}. הדגש: הזדמנות, ערך ללקוח. סיים עם "${DISCLAIMER}"`; return base+`כתוב פוסט כללי לדלק Ten.`; }
// ── Supabase ── המבנה האמיתי של הטבלה: id, year, month, ne (extraCtx), posts, created_at
import{SUPABASE_URL,SUPABASE_KEY}from"./constants.js"; const SB_HDR={"Content-Type":"application/json","apikey":SUPABASE_KEY,"Authorization":"Bearer "+SUPABASE_KEY};
export async function saveGantt(ganttData){ const id=`${ganttData.year}-${ganttData.month}-${Math.random().toString(36).slice(2,7)}`; try{ await fetch(`${SUPABASE_URL}/rest/v1/gantts`,{method:"POST",headers:{...SB_HDR,"Prefer":"return=minimal"},body:JSON.stringify({id,year:ganttData.year,month:ganttData.month,ne:ganttData.extraCtx||"",posts:ganttData.posts,created_at:new Date().toISOString()})}); }catch(e){console.error("save error",e);} return id; }
export async function loadGantt(shareKey){ try{ const r=await fetch(`${SUPABASE_URL}/rest/v1/gantts?id=eq.${shareKey}&select=id,year,month,ne,posts,created_at`,{headers:SB_HDR}); const d=await r.json(); if(!d[0])return null; const g=d[0]; return{month:g.month,year:g.year,extraCtx:g.ne||"",posts:g.posts||[]}; }catch{return null;} }
export async function listGantts(){ try{ const r=await fetch(`${SUPABASE_URL}/rest/v1/gantts?select=id,year,month,ne,posts,created_at&order=created_at.desc&limit=30`,{headers:SB_HDR}); const d=await r.json(); if(!Array.isArray(d))return[]; return d.map(g=>({id:g.id,created_at:g.created_at,data:{month:g.month,year:g.year,extraCtx:g.ne||"",posts:g.posts||[]}})); }catch{return[];} }
